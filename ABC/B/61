#include <bits/stdc++.h>
using namespace std;

int main(){
  int a,b;
  cin >> a >> b;
  vector<int> ans(a + 1);
  
  for(int i = 0; i < b; i++){
    int c,d;
    cin >> c >> d;
    ans[c]++;
    ans[d]++;
  }
  for(int j = 1; j <= a; j++){
    cout << ans[j] << endl;
  }
}
